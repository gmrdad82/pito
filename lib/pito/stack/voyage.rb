# frozen_string_literal: true

module Pito
  module Stack
    # Voyage embedding-API request usage (24h + current month).
    module Voyage
      PROVIDER = "voyage"
      extend ProviderCounts
    end
  end
end
