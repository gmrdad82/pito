<% content_for(:title, "new OAuth application") %>
<% breadcrumb(["settings", settings_path], ["OAuth applications", settings_oauth_applications_path], "new") %>

<div style="margin-bottom: 8px;">
  <h1>new OAuth application</h1>
  <p class="text-muted" style="margin-top: 2px;">
    pick the scopes this application can request. <code>client_id</code> and
    <code>client_secret</code> appear once on the next page &mdash; save them
    before you navigate away.
  </p>
</div>

<div class="pane pane--standalone">
  <%= render "form" %>
</div>
