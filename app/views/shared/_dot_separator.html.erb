<%# Inline dot separator for breadcrumb-action rows and similar inline
    bracketed-link runs. Mirrors the muted center-dot already used in
    the header/footer nav (`.nav-sep` rule); shipped as a partial so
    page templates don't hand-roll the markup. %>
<span class="text-muted" aria-hidden="true">·</span>
