<%# Phase 14 §2 — edit bundle form. The `bundle_type`,
    `igdb_source_type`, `igdb_source_id` fields are NOT exposed —
    those are immutable post-create per master-agent decision #3. %>
<% content_for(:title, "edit #{@bundle.name}") %>
<% breadcrumb([ "bundles", bundles_path ], [ @bundle.name, bundle_path(@bundle) ], "edit") %>

<div style="max-width: 600px;">
  <h1>edit bundle</h1>
  <%= render "form", bundle: @bundle, form_mode: :edit %>
</div>
