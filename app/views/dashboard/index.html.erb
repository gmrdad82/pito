<h1>dashboard</h1>

<% if @video_count > 0 %>
  <p class="text-muted"><%= @video_count %> videos across <%= @channel_count %> channels</p>
<% else %>
  <p class="text-muted">no videos yet. run <code>bin/rails db:seed</code> to load sample data.</p>
<% end %>
