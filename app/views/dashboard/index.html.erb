<% content_for(:title, t("dashboard.index.title", default: "home")) %>
<% content_for(:head) do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>

<%# Home — intentionally empty. Layout-first rebuild: panels added one by
    one via the demo→pick→implement→confirm flow. Use plan mode + chat
    to specify each panel. %>
<div class="pito-workspace home-empty"></div>
