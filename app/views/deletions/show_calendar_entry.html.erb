<% content_for(:title, "cancel calendar entries") %>
<% breadcrumb([ "calendar", calendar_root_path ], "cancel #{@items.length} #{@items.length == 1 ? 'entry' : 'entries'}") %>

<%= render "calendar_entry" %>
