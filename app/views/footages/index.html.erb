<% content_for(:title, "footages") %>

<div style="max-width: 1100px;">
  <h1>footages</h1>
  <p class="text-muted" style="margin-bottom: 8px;">footage rows are scoped to projects — open a project to see its footage pane.</p>

  <% if @footages.any? %>
    <table>
      <thead>
        <tr>
          <th>filename</th>
          <th>project</th>
          <th>kind</th>
          <th>resolution</th>
        </tr>
      </thead>
      <tbody>
        <% @footages.each do |footage| %>
          <%# `data-keyboard-row` opts each footage row into the global
              keyboard controller's list-row navigation (`j`/`k` move
              highlight). Footage has no per-row checkbox / bulk action on
              this surface, but the highlight still helps scan through
              the list. %>
          <tr data-keyboard-row data-keyboard-row-id="<%= footage.id %>">
            <td><%= link_to footage.filename, edit_footage_path(footage) %></td>
            <td><%= footage.project.name %></td>
            <td><%= footage.kind %></td>
            <td><%= footage.resolution || "—" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-muted">no footages yet.</p>
  <% end %>
</div>
