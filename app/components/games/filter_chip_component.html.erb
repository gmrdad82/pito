<%# Phase 27 §01b — single bracketed chip link. Pure GET-link
    navigation; no JS, no forms. %><%= link_to href, class: css_classes, data: { filter_token: token } do %>[<span class="bl"><%= label %></span>]<% end %>
