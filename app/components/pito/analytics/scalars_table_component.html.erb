<div class="pito-analytics-scalars grid grid-cols-[max-content_1fr] gap-x-4 gap-y-1">
  <% rows.each do |row| %>
    <span class="text-fg-dim whitespace-nowrap"><%= row[:label] %></span>
    <%= render(row[:trend]) %>
  <% end %>
</div>
