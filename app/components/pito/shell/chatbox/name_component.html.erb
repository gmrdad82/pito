<span id="<%= slot_id %>" class="contents">
  <% if named? %>
    <span class="text-purple pito-chatbox__name"><%= title %></span>
    <%= render(Pito::Shell::InlineSeparatorComponent.new) %>
  <% end %>
</span>
