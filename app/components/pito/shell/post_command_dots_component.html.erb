<style>
  .pito-comet {
    display: flex;
    gap: 4px;
    align-items: center;
  }
  .pito-comet .dot {
    width: 8px;
    height: 8px;
    background: var(--fg-faded);
    flex-shrink: 0;
  }

  /* 19.2 — Comet bounce: LTR 1.07s + RTL 0.36s (3× faster return) */
  .pito-comet .dot:nth-child(1) { animation: pito-c1 1.423s linear infinite; }
  .pito-comet .dot:nth-child(2) { animation: pito-c2 1.423s linear infinite; }
  .pito-comet .dot:nth-child(3) { animation: pito-c3 1.423s linear infinite; }
  .pito-comet .dot:nth-child(4) { animation: pito-c4 1.423s linear infinite; }
  .pito-comet .dot:nth-child(5) { animation: pito-c5 1.423s linear infinite; }
  .pito-comet .dot:nth-child(6) { animation: pito-c6 1.423s linear infinite; }
  .pito-comet .dot:nth-child(7) { animation: pito-c7 1.423s linear infinite; }
  .pito-comet .dot:nth-child(8) { animation: pito-c8 1.423s linear infinite; }

  @keyframes pito-c1 {
    0%               { background: var(--accent-cyan); opacity: 1; }
    15.74%           { background: var(--accent-blue); opacity: .7; }
    31.48%           { background: var(--fg-dim);      opacity: .4; }
    47.22%, 95.87%   { background: var(--fg-faded);    opacity: .2; }
    96.875%, 100%    { background: var(--accent-cyan); opacity: 1; }
  }
  @keyframes pito-c2 {
    0%, 8.375%       { background: var(--fg-faded);    opacity: .2; }
    9.375%           { background: var(--accent-cyan); opacity: 1; }
    25.115%          { background: var(--accent-blue); opacity: .7; }
    40.855%          { background: var(--fg-dim);      opacity: .4; }
    56.595%, 92.75%  { background: var(--fg-faded);    opacity: .2; }
    93.75%           { background: var(--accent-cyan); opacity: 1; }
    99%              { background: var(--accent-blue); opacity: .7; }
    100%             { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c3 {
    0%, 17.75%       { background: var(--fg-faded);    opacity: .2; }
    18.75%           { background: var(--accent-cyan); opacity: 1; }
    34.49%           { background: var(--accent-blue); opacity: .7; }
    50.23%           { background: var(--fg-dim);      opacity: .4; }
    65.97%, 89.625%  { background: var(--fg-faded);    opacity: .2; }
    90.625%          { background: var(--accent-cyan); opacity: 1; }
    95.875%          { background: var(--accent-blue); opacity: .7; }
    100%             { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c4 {
    0%, 27.125%      { background: var(--fg-faded);    opacity: .2; }
    28.125%          { background: var(--accent-cyan); opacity: 1; }
    43.865%          { background: var(--accent-blue); opacity: .7; }
    59.605%          { background: var(--fg-dim);      opacity: .4; }
    75.345%, 86.5%   { background: var(--fg-faded);    opacity: .2; }
    87.5%            { background: var(--accent-cyan); opacity: 1; }
    92.75%           { background: var(--accent-blue); opacity: .7; }
    98%              { background: var(--fg-dim);      opacity: .4; }
    100%             { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c5 {
    0%, 36.5%        { background: var(--fg-faded);    opacity: .2; }
    37.5%            { background: var(--accent-cyan); opacity: 1; }
    53.24%           { background: var(--accent-blue); opacity: .7; }
    68.98%           { background: var(--fg-dim);      opacity: .4; }
    83.375%          { background: var(--fg-faded);    opacity: .2; }
    84.375%          { background: var(--accent-cyan); opacity: 1; }
    89.625%          { background: var(--accent-blue); opacity: .7; }
    94.875%          { background: var(--fg-dim);      opacity: .4; }
    100%             { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c6 {
    0%, 45.875%      { background: var(--fg-faded);    opacity: .2; }
    46.875%          { background: var(--accent-cyan); opacity: 1; }
    62.615%          { background: var(--accent-blue); opacity: .7; }
    78.355%          { background: var(--fg-dim);      opacity: .4; }
    81.25%           { background: var(--accent-cyan); opacity: 1; }
    86.5%            { background: var(--accent-blue); opacity: .7; }
    91.75%           { background: var(--fg-dim);      opacity: .4; }
    97%, 100%        { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c7 {
    0%, 55.25%       { background: var(--fg-faded);    opacity: .2; }
    56.25%           { background: var(--accent-cyan); opacity: 1; }
    71.99%           { background: var(--accent-blue); opacity: .7; }
    78.125%          { background: var(--accent-cyan); opacity: 1; }
    83.375%          { background: var(--accent-blue); opacity: .7; }
    88.625%          { background: var(--fg-dim);      opacity: .4; }
    93.875%, 100%    { background: var(--fg-faded);    opacity: .2; }
  }
  @keyframes pito-c8 {
    0%, 64.625%      { background: var(--fg-faded);    opacity: .2; }
    65.625%          { background: var(--accent-cyan); opacity: 1; }
    75%              { background: var(--accent-cyan); opacity: 1; }
    80.25%           { background: var(--accent-blue); opacity: .7; }
    85.5%            { background: var(--fg-dim);      opacity: .4; }
    90.75%, 100%     { background: var(--fg-faded);    opacity: .2; }
  }
</style>
<div class="pito-comet">
  <div class="dot"></div><div class="dot"></div><div class="dot"></div>
  <div class="dot"></div><div class="dot"></div><div class="dot"></div>
  <div class="dot"></div><div class="dot"></div>
</div>
