<%= render(Pito::Segment::Component.new(accent: :orange, background:, id: dom_id)) do %>
  <div class="flex flex-col">
    <%# Outcome text — standalone follow-up message; no original body, no hairline %>
    <% if outcome_text.present? %>
      <span class="text-fg"><%= outcome_text %></span>
    <% end %>
  </div>
<% end %>
