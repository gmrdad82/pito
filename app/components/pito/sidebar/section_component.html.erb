<%# Sidebar section — orange-bold label + content + 24px bottom margin %>
<div style="margin-bottom: 24px;">
  <div style="color: var(--accent-orange); font-weight: bold; margin-bottom: 4px;"><%= t(@title_key) %></div>
  <%= body %>
</div>
