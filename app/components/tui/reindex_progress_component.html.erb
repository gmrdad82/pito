<span class="tui-reindex-progress"
      data-controller="tui-reindex-progress"
      data-tui-reindex-progress-brand-value="<%= brand %>"
      data-tui-reindex-progress-width-value="<%= INNER_WIDTH %>"
      role="status"
      aria-label="<%= brand %> reindex in progress">
  <%= initial_frame %>
</span>
