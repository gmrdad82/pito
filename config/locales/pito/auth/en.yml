en:
  pito:
    auth:
      enroll:
        success: |
          TOTP enrolled.

          To log in, add this code to your TOTP app (Google Authenticator, 1Password, etc.):

            %{otpauth_uri}

          Or enter the secret manually: %{seed}

          Then open PITO in your browser and type:

            /login <6-digit code from your app>
