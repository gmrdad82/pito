# `/config` — the /config usage panel (bare form — no provider argument given).
# pito-tui parity audit reference capture (against dev.pitomd.com).
name: tui-audit-config
base_url: https://dev.pitomd.com
viewport: { width: 1000, height: 1400, scale: 2 }
steps:
  - visit: /
  - login: true
  - command: "/config"
  - sleep: 6
  - shot: config.png
